<!-- Parcial CU20: inscripcion, evaluacion configurada y nota validada. -->
<div class="mb-3"><label class="form-label">Inscripcion</label><select name="id_inscripcion" class="form-select" required><option value="">Seleccione...</option>@foreach($inscripciones as $inscripcion)<option value="{{ $inscripcion->id_inscripcion }}" @selected(old('id_inscripcion', $nota->id_inscripcion) == $inscripcion->id_inscripcion)>{{ $inscripcion->postulante->ci ?? '-' }} - {{ $inscripcion->postulante->nombres ?? '' }} {{ $inscripcion->postulante->apellidos ?? '' }} / {{ $inscripcion->gestion->nombre ?? '-' }}</option>@endforeach</select></div>
<div class="mb-3"><label class="form-label">Evaluacion</label><select name="id_evaluacion" class="form-select" required><option value="">Seleccione...</option>@foreach($evaluaciones as $evaluacion)<option value="{{ $evaluacion->id_evaluacion }}" @selected(old('id_evaluacion', $nota->id_evaluacion) == $evaluacion->id_evaluacion)>{{ $evaluacion->gestion->nombre ?? '-' }} / {{ $evaluacion->materia->nombre ?? '-' }} / Evaluacion {{ $evaluacion->numero_evaluacion }}</option>@endforeach</select></div>
<div class="mb-3"><label class="form-label">Nota</label><input type="number" step="0.01" min="0" max="100" name="nota" value="{{ old('nota', $nota->nota) }}" class="form-control" required></div>
